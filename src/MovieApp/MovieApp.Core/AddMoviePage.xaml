<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:MovieApp.Core.Controls"
             x:Class="MovieApp.Core.AddMoviePage">

    <ScrollView>
        <StackLayout Spacing="4" Margin="6">
            <Label Text="Title"></Label>
            <Entry Text="{Binding Title}"></Entry>
            <Label Text="Overview"></Label>
            <local:ExtendedEditor Text="{Binding Overview}"></local:ExtendedEditor>
            <Label Text="Release date"></Label>
            <DatePicker Date="{Binding ReleaseDate}"></DatePicker>
        </StackLayout>
    </ScrollView>

</ContentPage>